# REQ-20260922-PIPELINE：自动化生产工作台原始需求

- 捕获日期：2026-09-22。
- 来源：当前 VideosBatch 会话中的用户文字及批注；下列相关原文保存在本文件，Git 历史保留后续修订。
- 适用：ADR-0004、0005、0006；无附件，无密钥、账号或用户素材，无删节改写。
- 模式：Write ADR。用户已确定产品目标与排除项；具体技术方案尚属 agent-proposed，不视为实施或部署授权。

<a id="u1"></a>

## U1 对齐对象

> 然后现在对齐一下frameflow 好吗？看看我们距离frameflow，在效率上和体验感上有啥可以优化的？

FrameFlow 为只读参照，改造对象为 VideosBatch；既有 ADR-0001 同样记录这一方向。不能把 FrameFlow 的待办或历史规划当作已交付事实。

<a id="u2"></a>

## U2 明确排除

用户对“真正的报价确认”的批注：

> 我们不做这个，不要碰这个。

解释边界：不新增、重做或扩展报价确认、费用展示、积分、计费策略、价格计算或相关确认流程。既有 QUOTE 合同及执行前校验原样保留，不以自动化为由绕过，也不借任务确认对话框夹带新报价功能。

<a id="u3"></a>

## U3 核心目标

> 我要的是极致的体验和操作手感，比如整个任务支持多线推进，多任务，自动执行，多管理。等等，做成一个自动化的流水线的那种。

<a id="u4"></a>

## U4 实时、编辑和文档要求

> 还有就是实时渲染和反馈，支持编辑保存，推进等。做好这些ADR吧。

用户同时明确调用 `adr-driven-delivery`。本次产出决策提案和分阶段验收合同；不启动业务实现。

## Discussion Basis

- 已沟通方向：多个项目互不锁住、无依赖工作并行、后台持续执行、统一管理、必要确认处集中介入、局部恢复。
- 用户最新补充强调实时渲染、反馈、编辑、保存和推进，因此不能只交付后台队列而忽略编辑交互，也不能只做看板外观。
- 上述是目标讨论基础，不表示用户逐项批准数据库选型、并发数、性能阈值或所有产品细节。
- 技术提案：保留九步业务合同，以持久化运行记录和事件支撑调度；实时显示可核验结果；编辑与执行版本隔离。详见三份 ADR。

## Evidence Baseline

VideosBatch `6cc8174`；本轮只读复核 API、runner、CinemaStore、编辑器和任务页。FrameFlow `7d7af229` 的比较沿用同会话上一轮源码证据，非生产或真实模型性能对跑。

- 已有：检查点、重启中断标记、Provider taskId/执行快照、版本冲突拒绝、本地草稿、ETag 和可见性轮询策略。
- 缺口：同步执行 API、进程内 flight map、无统一持久化任务调度/事件续接；批量媒体执行主要串行；草稿为当前标签页存储。
- 不复述“所有工作都须重做”或“关闭页面一定导致服务器停止”等不符合现有代码的判断。

## Later Clarifications

尚无。后续用户澄清按日期追加，不改写 U1—U4 原文。
