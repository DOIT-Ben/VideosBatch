# 2026-09-13

## H3 视频通道真实验收（大哥拍板：不用 Seedance，用 H3 跑一个视频即可）

- 大哥指示：Tier 3 视频验证走 **NewAPI H3**（`minimax_h3`），只跑一段。
- 路径确认：workflow 的 STITCH 门禁要求全部 12 段 ready，不能只跑一段；
  正确路径是 **SeeReel 单 shot API**：`POST /api/shots/:id/generate`，
  `.env` 的 `VIDEOSBATCH_VIDEO_PROVIDER=newapi-h3` 使 generators.ts:1075 自动路由到
  `generateShotVideoViaNewApiH3`（newApiH3Video.ts）。
- 选 shot_8e91313e（3 资产：小明/数学老师/教室，投影阶段已持久化
  `videosBatchReferenceBindings` 快照，正好满足 H3 ≥2 张参考图门槛）。
- **前置坑**：H3 多参考图模式强制 2–9 张真实图（HTTPS 或本地 /media/ 的 PNG/JPEG/WebP）；
  fake 会话资产无图。先 `POST /api/assets/:id/generate` 出图——**必须显式传
  `model:"gpt-image-2-1k"`** 才走 lyaiapp 通道（.env 已配 IMAGE_PROVIDER=lyaiapp，
  但该 API 默认走 Seedream，凭据为空时静默返回 placehold.co 占位图，elapsed 0s 是假成功的信号）。
  3 张图各 23–30 秒，落 imageUrl/mediaUrl 本地 + sourceImageUrl 远端。
- **结果**：H3 提交 → 轮询 191 秒 → `/media/videosbatch-h3-shot_8e91313e-*.mp4`，
  10.13s h264 4.74MB，shot.status=ready。产物复制到 output/real-acceptance/h3-shot_8e91313e.mp4。
- 全链真实闭环达成：真实 LLM 文本（terra/deepseek）→ 真实 MiniMax TTS → 真实参考图
  （lyaiapp gpt-image-2-1k）→ 真实视频（NewAPI H3 minimax_h3）。
- H3 幂等/安全设计已验证存在：Idempotency-Key、提交状态未知拒绝重提（防重复计费）、
  参考图 hash 快照一致性校验、任务号 checkpoint 续查。

## 对照《视频制作工作流完整步骤》补齐提示词缺口（09:26）

- 用户拿 ShanHaiEdu《视频制作工作流完整步骤.md》对照 8 个外置骨架；按「只看内容与意图、
  无视命名/编号/JSON 形态」标准筛出 7 个内容级缺口，被批评「只列不补」后全部补进骨架：
- **screenplay.md** 新增「导演要求」段（7 条）：黄金开场 15 秒、可视化原则（visualAction
  必须可拍摄）、节奏把控、台词语气通过文字体现（不加格式标签，防 TTS 念出）、假说标注、
  记忆锚点（最后一场 voice 含金句/口诀但不给答案，与悬念结尾并存）、内容底线。
- **final-storyboard.md** 新增规则 8/9/10 + 自检扩展：信息控制与拥挤处理（一分镜一冲突、
  装不下顺延）、景别配额（每分镜恰 1 处特写）+ 三类运镜词库、SCIENCE 数学可视化四分法
  （数量/图形/计算/测量各配手法，测量必须出现具体工具）、KNOWLEDGE 稳妥表达（「人们曾经」）。
- **asset-plan.md** 补按类成品自检 + 可选资产逐项说明。
- 有意不做：小结/思考题/下集预告独立 JSON 字段（需动 schema 与合同校验器，金句走 voice 已够）。
- 验证：tsc + prompt-templates + frameflow-canonical + llm-text-stages 全绿（锚点未破坏）。
- 坑重申：bash -e 内联中文/多行会转义炸裂，写文件类操作一律走 Write/Edit 工具。

### 二轮补漏（09:33）

用户追问「其他地方呢」后复查出 3 处漏项，全部补齐：
- **final-storyboard.md** 规则 11/12：三类特效词库（STORY 柔光/抖动/局部高亮/问题符号弹出，
  SCIENCE 箭头动效/结构分层/时间流速示意，KNOWLEDGE 关键词浮现/时间轴闪烁/古今分屏）+
  镜头衔接连贯（动势/情绪/剧情一致；SCIENCE 逻辑链递进；KNOWLEDGE「问题—过程—悬念」过渡）。
- **screenplay.md** 导演要求 8：visualPresentation 选型枚举（动画示意/历史场景再现/实拍演示/
  图表数据/角色扮演，可「+」组合）+ 单场不过长。
- tsc + 三个提示词 smoke 复验全绿；随 commit 229b9ee 之后的补充提交一并入库。

## 子智能体双审查 + 三轮修复（09:37）

用户要求子智能体详细审查。并行派两个 Explore agent：
- **Agent A（文档↔骨架缺口复核）**：逐条比对后判 5 个阶段仍有 17 条残留（高优 6 条：
  导入缺 120—180 秒预期/缺信息出口/子方向替换/五要素；分镜缺「制作可行性禁大场面」整块/
  SCIENCE 知识准确红线；故事缺「不提前讲透」；垫图缺超 7 个取舍标准），仅第五步基本无缺口。
- **Agent B（新规则 vs 代码校验一致性）**：12 条新规则 11 条 ✅；**A5 金句 ⚠️**——
  repair checklist（llmTextStages.ts L907）硬性「voice 合计 1 句」与主骨架 1—2 句矛盾，
  最后一场分镜一旦触发 repair 金句必被清掉；sanitizer 中段清空（L391-395）是第二丢失路径。
  另确认：剧本 dialogue 不进 TTS（只消费分镜 voice）、visualPresentation 是自由 string、
  camera/visual 无长度上限、schema additionalProperties:false 所以自检文本不能新增字段。

**三轮修复（全部落地）**：
1. 代码：L907 repair checklist 放宽为「合计 1—2 句（至多两个子镜头各一句）」与主骨架对齐。
2. 骨架加金句兜底：规则 7 补「金句作为该分镜 voice 第一优先句子；悬念优先写 visual」。
3. Agent A 高+中优缺口全补：final-storyboard 规则 9 景别枚举 14 项、KNOWLEDGE 运镜补
   「地图路径生长」、规则 10 SCIENCE 准确红线、规则 12 补「现象—思考」、新规则 13 制作
   可行性（禁大场面/电影级特效/长镜头）+ 分类安全细则；intro-candidates 补 120—180 秒
   预期、缺失项出口、子方向同大类替换、正文五要素、真实性依据一句话、稳妥话术；
   story-script 补不提前讲透/虚构不伪装/「逐渐发现」话术；asset-plan 补遗漏检查七项枚举、
   场景小型/大型分治；copyable-prompt 补超 7 个取舍标准（主角/动作道具/首现场景优先）；
   screenplay 补音效可空置、名言原样保留、中文输出。
4. **有意裁剪记录**：知识模块包装的小结/思考题/下集预告三项不做（金句走 voice 已覆盖，
   加字段需动 additionalProperties:false 的 schema）；语气标注制→文字体现（防 TTS 念标签）。

验证：tsc + prompt-templates + frameflow-canonical + llm-text-stages + native-projection 全绿。

## 对照用户修订版工作流文档，同步口径（10:07）

用户修订了 ShanHaiEdu《视频制作工作流完整步骤》（统一修订版，头部五条统一口径）。
逐条对照后：单故事线、ID 只进垫图层、分镜/垫图格式分离、风格唯一四条我们本已对齐；
同步了 3 处我们同样存在的歧义：

1. **字数口径歧义**：导入「200—300 字」未说明算不算名称/悬问/真实性说明 → 改为
   「导入正文 200—300 字（字数只算正文，不含导入名称、结尾悬问与真实性说明）」。
2. **文学化边界缺失**：文档新增「对话、场景和心理活动可合理文学化，但不得编造不存在的
   重大发明、作品、身份、经历或成就」→ 导入与故事两骨架的真实性条款都补上该枚举。
3. **方向优先级缺失**：文档新增「方向优先级」节（优先 A/B 历史素材，C 类必须服务知识）
   → 导入骨架硬约束 5 补「优先选用 A、B 两类……C 类不得只追求热闹」。
4. **风格统一遗漏**：文档口径 1「全流程唯一风格」，但我们的剧本/分镜骨架原来只字未提风格
   → 两骨架唯一真源补「全片画面统一使用影视级 3D 国漫 CG 风格，不得混入其他风格」，
   分镜自检补风格项。

**文档残留问题（已提醒用户，未代改）**：a) 导入步写「不扩写 500—800 字视频剧本」，
故事步写「600—800字」，两者冲突（我们骨架统一用 600—800）；b) 「从推荐 3 套中选定
1 套」未写明由人选还是模型选（我们产品是人选后锁定）；c) 「记忆锚点：每段结尾用金句
口诀」与「最终结尾必须留下课堂问题、不提前讲透」仍有张力——每段都上金句会吃掉 voice
预算且有剧透风险；我们的口径是金句仅最后一场（有意裁剪，已记录）。

验证：tsc + prompt-templates + frameflow-canonical + llm-text-stages 全绿。

## 骨架整体优化（09:52）

用户要求对 6 个主链骨架做整体优化（补齐后的质量收口），不是再加内容：

- **统一章节结构**：角色定位 → 安全边界 → 唯一真源 → 硬约束 → 阶段专业要求 → 优先级 →
  自检 → 输出格式。分镜骨架从 13 条平铺改为「结构硬约束（1-5）/ 叙事硬约束（6-9）/
  镜头语言（10-13）/ 制作可行性与安全（14-16）」四组。
- **拆长句**：导入骨架硬约束 2 一句塞四件事拆成 2/3/4 三条；分镜原规则 7（voice+音效+
  同步）拆成 7（voice 预算）/8（sound 与同步）两条。
- **去重**：分镜结尾「不替学生讲完答案」原文重复两次（规则 6 与尾行），合并；尾行三件事
  （小学安全/结尾/输出格式）分流到 15/16/输出行。
- **标点统一**：全部骨架弯引号「“”」→ 直角引号「」。
- copyable 的「超量取舍」从尾部游离段提为硬约束第 6 条。
- 锚点全保留（600—800字/影视级 3D 国漫 CG 风格/VIDEO_SCREENPLAY/FINAL_10_SECOND/
  targetDuration/10/垫图可复制提示词副本/referenceAssetIds/每个语义标签只能出现一次/
  其余子镜头 voice 一律写「无」）。坑重申：prompt-templates smoke 对「600—800字」做
  includes 无空格匹配，优化文案时数字与汉字之间不能随手加空格。

验证：tsc + prompt-templates + frameflow-canonical + llm-text-stages + native-projection 全绿。
