# 2026-09-16 工作日志

## 计费/账务语义线 —— 收口

用户明确表示「不用管计费」，据此**收口计费线**，不再往下钻。后续再有人问「是不是关了计费」
可直接引用下面的查证结论，不必重新审。

**澄清（查证结论，非记忆推测）**：

- 本仓库**不存在「计费开关」**：全仓 `src/` 扫 `billingEnabled / disableBilling / NO_CHARGE /
  freeMode / skipBilling` 零命中。所谓「关计费」这个动作在本仓无处可执行。
- 前两轮（`a7f6d49` / `da17298`）改的是 **`billingResult` 结论字段的判定与合并**，属**花钱侧
  风控**（防「可能已花掉的钱被记成没花 → 系统再提交一次付费请求」），**不是**接支付、改价、
  动额度。碰过的文件无一是开关/凭据/额度文件。
- `runtimeProvider.ts`（模式唯一入口）最后一次改动为 `cc18dfc`（死代码清理），不在前两轮内。
- 当前运行时如实报 `executorMode=fake / mediaMode=fake / ttsProvider=fake`（`runtimeProvider.ts:74/79`
  的 `|| "fake"` 默认值，仓库原有约定，非人为设置）→ 零付费调用。真出片需双开
  `MEDIA_MODE=native` + `EXECUTOR_MODE=llm`，复用 SeeReel 凭据，切换前须用户点头。

**仍挂着（均与计费无关）**：D3（内联 data URL 参考图，待裁定）、D9（执行快照载荷哈希自校验层，
需授权）、P3（`promptDocument` 结构化提示词，需单独 ADR）、推送远端 / 并入 `master`（未授权）。

---

## ADR-0001 第三轮执行：D3 + D9 落地（用户「全部授权。开始」）

### D3 参考图来源扩展（`3a5441b`，spec 1.4.7）

- `referenceCandidates()` 原先只放行 `^https://` 与 `/media/`，内联 `data:image/...;base64,...` 被
  **静默过滤**。现已三来源等价（HTTPS / `/media/` / 内联 `data:`）。
- 关键纪律：内联载荷**按 base64 长度预判 20MB**（在 `Buffer.from` 分配前拒绝）；复用同一
  `referenceBytes()` 保证内容寻址口径一致；提交地址改写为 `inline:sha256:<bytesSha256>`，
  **原始 data URL 既不保留在内存也不落库**。

### D9 付费执行快照完整性层（`5ca2235`，spec 1.4.8 / 新 §7.12）

- 新模块 `src/server/videosBatchWorkflow/executionSnapshotIntegrity.ts`：严格规范化 JSON
  （键递归排序、数组保序、`undefined`/非有限数字/非 JSON 值**硬失败**）、载荷 SHA-256、
  读取时重算 + 逐字规范形式校验、适配器能力矩阵闸
  （`(adapterKey, adapterVersion, runtimeModelId, creationMode, 参考图数量)`）。
- **⚠️ 刻意并存不替换**：`canonicalStoryboard.stableJson`（`JSON.stringify` 投影）**保留不动**——
  它服务血缘内容哈希，改它会重写全部已钉历史哈希。新模块只约束**付费快照**。这条边界
  写进了 ADR-0001 §13 与 spec §7.12。
- 接线：`onExecutionSnapshotPrepared` → `Shot.videosBatchExecutionSnapshot`；恢复任务先校验，
  适配器下架或载荷被改一律拒绝。适配器闸在**取参考图之前**执行。
- `executionSnapshotIntegrity.ts` 是叶子模块（只依赖 node:crypto），可被 shared 层安全引用。

### P3：转为独立 ADR，**未实现**（重要结论）

- FrameFlow 的 `VideoPromptDocument` **依赖正文里有可标记的引用锚点**（其教学原型用
  `【P###-A###】`）。本仓 spec §7.7 **明文禁止稳定 ID 进入 H3 prompt**、正文只用语义标签。
- 所以机械照搬只会产出**单 text 节点的退化解**——投影一致性恒真、零信息增益，属于「改了个寂寞」。
- 已写 `docs/adr/0002-videosbatch-structured-prompt-document.md`：四方案对比、P3-1～P3-4 分阶段、
  **三个待产品确认的前提问题**。答案到位前维持扁平字符串（有意为之，非遗漏）。

### 验证与提交

- `tsc --noEmit` 通过；`npm run verify:offline` **RC=0（4m08s，~120 smoke + 构建）**；
  `smoke:specs` / `smoke:videosbatch-doc-consistency` 通过。
- 提交：`3a5441b`（D3）、`5ca2235`（D9）、ADR 记录（见下）。

---

## 第四轮：主动 bug 扫查（真实浏览器跑 9 步）

方法：仓库内 `playwright-core` 1.62.1 + 本机 chromium
（`C:/Users/HB/AppData/Local/ms-playwright/chromium-1234/chrome-win64/chrome.exe`）headless 驱动真实 UI，
交叉读代码。**关键教训：happy path 全绿 ≠ 没 bug——这轮两条真缺陷都在「回退/异常路径」上，
截图干净、零 console 报错，全都测不出来。**

### F1【高·功能死锁】`stale` 被当成阻断条件 → 回退过任何一步就再也跑不完

- 重现：`POST /api/sessions/<id>/videosbatch/restart-from/SCREENPLAY` 后 `run-all` **连打 4 次都是
  200 且 `currentStage` 不变**，`FINAL_STORYBOARD` 永远 `stale`、`completed` 永为 false。
- **无出口**：`retry` 只认 `failed`（409 `STAGE_NOT_FAILED`）；在卡死那步再点「重新生成本步骤」也无效
  （`restart-from` 置 `pending`，但 mismatch 判定**不受 `requiresLineage` 门控**）。
- 根因：`runner.ts` 的 `dependencyIssues()` 把「依赖未就绪」与「本步 lineage 过期」合并成一个阻断清单。
  `restart-from` 为保留可检视的历史产物**不删下游 artifact** → 下游 `sourceHashes/sourceRevisions`
  永远落后于刚重跑的上游 → `runNext` 每次把自己重判成 stale 并 `return`，游标不动。
- 修法：**只删两处 mismatch 判定**；「依赖未 `ready`／依赖 `contentHash` 不自洽／本步 lineage 完全缺失」
  仍阻断。`stale` 回到「重跑信号」语义。spec 升 **1.4.9**。
- **⚠️ 别误伤守「缺失 lineage」的用例**：`smoke:videosbatch-native-media-resilience:433` 断言伪造的
  `ready`（无 stage 级 `sourceHashes`）必须判 `stale`——那是 **missing 不是 mismatch**，必须继续阻断。
- `smoke:videosbatch-runner` 原用例**只断言了下游被标 `stale`，从未断言过之后能否恢复**——这正是缺口；
  已补两组回归（回退后必须能重跑到完成 + 新血缘一致；对已 stale 的阶段再 `restart-from` 必须能恢复）。

### F2【低】缺 favicon → 每次加载 console 报 404

`index.html` 未声明图标，浏览器默认请求 `/favicon.ico`；仓库已有 `public/seereel-mark.png`。
加 `<link rel="icon" type="image/png" href="/seereel-mark.png" />` 后整页 console 错误数 **0**。

### G1【未修·需裁定】VideosBatch 工作台整块未接 i18n

头部 ⋯ 有「Switch to English」，切了之后 `document.lang` 与 `localStorage["uiLanguage:v2"]` 都正确变化，
**同一会话的「制作画布」也确实变英文**，但「流程制作」9 步工作台**一个字符串都不变**。
`src/client/i18n.tsx` 只覆盖 SeeReel 词典；`src/client/videosBatchStudio/` **零 `useI18n`**
（21 个文件 1759 中文字符）。整块本地化是产品级范围决策，**未动**。

### 复核通过、刻意未改

- `/api/sessions/:id/videosbatch` 对未启动工作流的会话返回 409 `WORKFLOW_NOT_STARTED`：设计内，客户端有空态兜底。
- `projectId` 服务端要求 `^P\d{3,}-A\d{3,}$`，UI 固定发 `P001`：单项目产品下 `publicAssetId` 全局唯一，非缺陷。
- 历史会话 3 处 `failed`（LLM 超时 / `omissionCheck` / `COPYABLE_PROMPT PARTIAL`）均来自 2026-09-01
  真实模式运行，非当前代码活缺陷。

### 探针脚本（`output/` 被 gitignore，删了要重写）

`probe-1.mjs`（单页信号+404 来路）｜`probe-walk.mjs`（逐步骤抓异常+截图）｜`probe-full.mjs`（API+browser
混合跑完整链路）｜`probe-dump.mjs`（步骤按钮结构）｜`probe-ui.mjs`（⋯ 菜单/画布切换/上一步）｜
`probe-i18n.mjs` / `probe-i18n2.mjs`（语言切换，后者带 `addInitScript` 预置 localStorage）｜`probe-routes.mjs`。

### 落地

- 修复提交 `4c29132`（分支 `feature/videosbatch-audio-readiness-gate`）：spec→1.4.9、`runner.ts`
  删两条 lineage-mismatch 阻断、`smoke-videosbatch-runner.ts` 新增「回退后 runAll 必须恢复」回归、
  `index.html` 补 favicon。`verify:offline` 全绿 RC=0。
- 教训已沉淀进技能 `videosbatch-stage-development` → 坑 2b：lineage 落后 ≠ 阻断，stale 是重跑信号；
  死锁只会在「回退 + 再 run-all」这条 happy path 之外的路上出现。

## 第五轮：深度全面 bug 扫查（三路并行 + 真机复现）

**已真机确证（会话 ses_582f90e2，`restart-from EXECUTION` → `run-next`）：**

- **F3【高·已确证·白屏】AUDIO_DELIVERY 不在 9 步产品表里 → 整页崩溃。**
  `stageModel.ts:33-43` 的 `VIDEOS_BATCH_PRODUCT_STEPS` 只映射 13 个阶段，**缺 `AUDIO_DELIVERY`**
  （阶段表共 14 个）；`productStepForStage`（:49-53）对未映射阶段直接 `throw`。
  `VideosBatchStudio.tsx:117` 顶层就调 `deriveCurrentProductStep`，而 `src/client` **无任何
  ErrorBoundary**。runner 在阶段成功后无条件推进游标（`runner.ts:473-475`），所以
  **EXECUTION 成功 → currentStage=AUDIO_DELIVERY**。
  实测：`#root` innerHTML 长度 **0**、工作室节点 0、body 文本 0，pageerror
  `No product step configured for VideosBatch stage AUDIO_DELIVERY`。
  **触发面**：①第 8 步「视频生成」点「生成下一步 →」（正常 UI 操作）；②自动运行中
  AUDIO_DELIVERY 失败（`runner.ts:504` 停在该阶段）；③API `restart-from/EXECUTION`。
  **只能靠 API `run-all` 救回，UI 救不回**（页面全白、无入口）。`/api/state` 的会话列表仍正常
  显示进度（走不抛错的 `deriveProductStepStatus`），用户会以为只是打不开。

- **F4【高·已确证·native】`mixFakeAudioTimeline` 丢弃 `startSec`，全部语音叠到 0 秒。**
  `audioDelivery.ts:107-113` 对每路输入只做 `apad,atrim=0:durationSec` 后直接 amix，**没有
  `adelay`**；而同仓库正确写法在 `narration.ts:1479-1482`（`adelay=<startSec*1000>|<..>`）。
  事件确实有绝对位置：`buildAudioTimeline`（`nativeMediaStages.ts:480-524`）按 effect 时长推进
  cursor。门禁（`:591-644`）只验时间范围与 URL 形状，一律放行；fake TTS 是静音所以永远全绿。
  （历史「有声成片」同型事故的复发：门禁验的东西下游没真正消费。）

- **F5【高·已确证·native】AUDIO_DELIVERY 复用旧音频只认 `eventId`。**
  `nativeMediaStages.ts:666-669、687-695`：`previousByEvent` 仅按 `eventId` 索引，
  `status==="ready" && isUsableAudioUrl` 即复用，**不校验 text/start/end**。事件 id 形如
  `shot-<seq>-voice-<idx>`，改台词后 id 不变 → 成片沿用旧文本音频。复用路径**绕过**了
  `synthesizeFakeSpeech` 里按 text 做的内容寻址文件名。

**已确证（代码级，未真机复现）：**

- **F6【中】`dependencyIssues` 的 contentHash 校验是死代码。** `runNext` 先
  `hydrateArtifactHashes`（`runner.ts:46-52、351`）按 artifact 重算并覆盖 `contentHash`，
  再做依赖校验（:393），所以 `source.contentHash !== calculated` 恒为假，artifact 被就地篡改
  也发现不了。F1 修复后这本是「依赖未变」的唯一哈希判据。
- **F7【中】`store.load()` 解析失败静默清空。** `store.ts:36-38` `catch { this.data = emptyStore() }`，
  不抛错、不备份、不告警；之后任意一次 save 就把空 store 原子覆盖掉
  `data/cinema-store.json` → 全部会话/分镜/资产永久消失。
- **F8【中】`withWorkflowFlight` 按 `kind` 去重会丢并发写。** `api.ts:51-53`
  `if (existing?.kind === kind) return existing.promise`，第二请求体被静默丢弃。kind 是
  `artifact:${stageId}`（按阶段区分，比初判窄），风险在**同一阶段并发保存**（自动保存 vs 手动保存）。
- **F9【中】调试抽屉把「保存失败」渲染成成功。** `ArtifactDebugDrawer.tsx:29-38` `await onSave()`
  后无条件 `setEditing(false); setError("")`；而 `VideosBatchStudio.tsx:389-392` 的 onSave 走
  `perform`，**吞异常返回 undefined** → 抽屉显示「已退出编辑」；真错误只写进 `.vbs-inline-error`，
  位于 Dialog 遮罩之后不可见。
- **F10【中】`isManualGateReady` 不检查 `ASSET_CANDIDATES.status`。** `assetConfirmationReady`
  （`runner.ts:155-188`）只比对 plan/candidates 条数与 assetKey，且 SCREENPLAY 的依赖表不含
  ASSET_CANDIDATES → 条目恰好对齐时可用过期素材推进。（对比 `replaceStageArtifact:560` 是查了的。）
- **F11【中低】无 registry 阶段的 artifact 零校验。** `replaceStageArtifact:563` 仅在
  `registry?.[stageId]` 存在时校验；LESSON_INPUT 在三个 registry 里都没有 → 可写入
  `{"artifact":null}`，`runner.ts:357` 只判 `=== undefined` → null 被当有效。
- **F12【低】`restart-from` 打手动门会卡住。** `restartFrom("COURSE_INTRO_SELECTION")` 置 pending，
  而 `introSelectionReady`（:191）要求 `status==="ready"` → runNext/runAll 直接返回，只能重新提交选择。
- **F13【低】资产计划按 category 精确分组**（`AssetPlanStage.tsx:12-17,30`）；**资产卡「已确认」
  徽标不看 stale**（`AssetGalleryStage.tsx:59` vs `:43-45` 同页自相矛盾）；**`StoryboardStage.tsx:64`
  的 setTimeout 未清理**；**自动运行期间进度条全程凝固**（服务端单请求、循环内不落库，
  5s 轮询拿不到中间态）。

**方法学教训**：这轮三个真·高危全部在**非 happy path**（回退后单步推进、native 才走到的混音、
失败路径）——纯走查和截图全绿。**「代码级确证」和「真机复现」要分开标注**，前者会误报（本轮
agent 报的 kind 去重影响面就比实际大）。探针：`output/probe-audio-gap.mjs`、
`output/probe-white-screen.mjs`（API 造态 + 浏览器验白屏，证据图 `output/bug-audio-delivery.png`）。

---

## 第五轮：F3–F13 修复落地（"修复啊" · 同日）

**结论**：上表 14 条全部处理完毕。其中 F3/F4/F5/F12 是**真·功能缺陷**（白屏 / 音频错位 / 陈旧音频复用 / 闸门回退失效），F6–F11/F13 为健壮性与一致性。spec 仍 `1.4.10`（未提交，新增条款并入同一条目）。**扩展后的 `verify:offline` RC=0**。

**逐条落地（文件级）**
- **F3 白屏**：`stageModel.ts` 把 `AUDIO_DELIVERY` 归入「最终成片」；新增 `StudioErrorBoundary.tsx` 包住工作台视图（渲染期异常降级成「这一步暂时无法显示 + 刷新」，**永不再白屏**）；spec §0.1 合并重复的 08 行、计数订正 14（含 §0.3 标题的「十三个」）。
- **F4 混音定位**：`audioDelivery.ts` 新增 `buildAudioMixChains()`——逐输入 `aformat=...stereo{,adelay=ms|ms},apad,atrim=0:<dur>`；声明源数与可解析源数不符时抛 `AUDIO_MIX_SOURCE_UNRESOLVED`（失败关闭）。
- **F5 复用陈旧音频**：`nativeMediaStages.ts` 增 `previousIntentByEvent` + `matchesRecordedIntent(event)`，复用须满足「文本 + 时间窗一致」且 URL 可用。
- **F6**：`runner.ts` `hydrateArtifactHashes` 改 **fill-only**。原实现每次用当前产物覆盖落库哈希 → 「依赖哈希是否失配」**恒等**、不可伪证。安全性先审计 14 个会话（103 阶段、0 失配、仅 13 处历史缺失）。
- **F7**：`store.ts` 区分 `ENOENT` 与解析错误，后者 `rename` 成 `cinema-store.json.corrupt-<ts>` 留证 + `console.error`，再以空库启动——不再静默销毁用户数据。
- **F8**：`api.ts` 增 `writeFlightKind(prefix, body)` = `` `${prefix}:${contentHash(body)}` ``，`artifact:` / `retry:` 飞行键含内容哈希，同阶段不同内容不再互吞。
- **F9**：`VideosBatchStudio.tsx` 增 `performOrThrow(label, op)`，调试抽屉与结构化产物保存**失败即抛**，不再渲染成成功。
- **F10**：`assetConfirmationReady` 增 `ASSET_CANDIDATES.status` / `ASSET_PLAN.status` 检查（陈旧上游不得通过确认闸）。
- **F11**：LESSON_INPUT 的 `null` 判为缺失（`artifact === undefined || === null`）；`PUT .../artifact` 拒绝 `body.artifact === null`（`ARTIFACT_REQUIRED`）。
- **F12【重要·重新定性】**：真实缺陷**不在课程导入闸门**，而在**资产确认闸门**。两个闸门的「已答复」来源不同：课程导入来自工作流级字段（有 `clearIntroSelection`），资产确认来自**阶段产物自身**（`confirmed:true`）——`restartFrom` 只把 status 改回 `pending`、产物原样保留 → **闸门没重新武装**，「重新生成本步骤」像空操作且自动运行直接冲过确认。修：新增 `clearAssetConfirmation()`（删产物 + `contentHash`），与 `clearIntroSelection` 对称。契约写入 spec **§7.13**。
- **F13**：AssetGallery 徽章自相矛盾（改为 `!needsConfirmation && artifact.confirmed && selectedId`）/ AssetPlanStage `categoryKey()` + `CATEGORY_ALIASES`（**子串匹配 + 原值兜底**）/ StoryboardStage 保存 try-catch + 复制定时器 `useRef` 清理 / ScreenplayStage try-catch / `statusForStep` 在 `runInFlight` 时返回 `running`（自动运行不再像卡死）。

**⚠️ 质量基建缺口（本轮新发现，比上面任何一条都值得记）**
8 个 VideosBatch smoke **从未进入 `verify:offline`**，其中 `smoke-videosbatch-product-ui-foundation.tsx` **在 `package.json` 里连脚本条目都没有**（纯孤儿文件）。也就是说 **F3 的映射完整性守卫、F4/F5 的音频守卫全都跑不到**——修完等于没有回归网。逐条比对「已注册 vs 在链内」另查出 6 条同类漏挂，**直接运行全部通过**（不是红了被摘掉）。已注册孤儿 + 8 条并入链；**刻意排除** `smoke:videosbatch-real-text-server`（占**固定端口**且拉子应用进程，覆盖已被 `e2e` 涵盖）。`runtime-provider` 用 `listen(0)` 取临时端口，可安全入链。
**教训：「写了 smoke」≠「有回归网」。新增守卫必须同时确认它出现在 `verify:offline` 链里。**

**方法学**：每条发现**先写探针**（`output/probe-f12-restart-gate.mjs` 等，均 gitignore）取「改前必红 / 改后必绿」，F12 的阳性对照就是探针自身的改前改后输出。`tsc --noEmit` 与 `--noUnusedLocals` 均 RC=0（顺手清掉 `newApiH3Video.ts` 一处历史未用导入）。

---

## 第六轮：审查上一轮的修复（用户「审查你的修复」→「修复」）· spec 升 1.4.11

**结论先讲**：上一轮 14 条修复里有 **3 处「看着修好了、实际没修到」**，其中 2 条是我自己引入或漏掉的真缺陷。最深的根因是 **用「加了守卫」替代了「确认守卫能失败」**：三条最关键的新守卫里两条**在结构上不可能变红**，还有一条**根本没有测试**。`verify:offline` 全绿只证明「没跑挂」。

- **A【中·逃生口被自己吃掉】** `VideosBatchHeader`（「任务列表」）渲染在**错误边界内部** → 边界接管后头部一并消失，面板只剩对确定性错误刷新即复现的「刷新页面」。上一轮我推断「App 头部在边界外可逃生」，被探针直接否证（「任务列表」计数 = 0）。修：`StudioErrorBoundary` 接 `onBackToSessions`，兜底面板用**真实头部 class**（`vbs-v2-header`/`vbs-v2-back`）重渲染出口，「返回任务列表」为主动作；`VideosBatchStudio` 转发。**零 CSS 新增**。
- **B【中·闸门第三处缺口】** `introSelectionReady` 一直校验自身 status，**`assetConfirmationReady` 完全不校验 `ASSET_CONFIRMATION.status`** → 上游重跑后确认阶段变 `stale`，界面要求重新确认、服务端却判定闸门通过、自动运行直接冲过。这是 F10/F12 的**同一个不变量的第三个面**。修：补 status 校验；断言含 `stale` 不就绪 + 重新确认可恢复（不引入 §14 F1 式死锁）。
- **C【中·守卫不可伪证】** `renderToStaticMarkup` 是 SSR，**React 在 SSR 不捕获边界**（实验：游标指向不存在阶段 → THREW）→ 上一轮那条 `!includes("这一步暂时无法显示")` **结构上不可伪证**。修：删装饰断言；渲染循环改显式 try/catch + 断言完整外壳；兜底面板改**直接单元断言**（实例化边界 + 置 state + 渲染 `render()`）。**改完第一版又空转了一次**：面板文案里正好含「请先返回任务列表」，`includes` 命中的是段落而非按钮 → 改按 `class` 定位 `<button>` 再比对文本。
- **D【中·我漏修的 F9 同类缺陷】** `StoryStage.save()` = `await onSaveContent?.(draft); setEditing(false);` 而 `saveStoryContent` 走**吞异常的 `perform`** → 保存被拒后编辑器照样关闭、编辑**静默丢失**、界面显示已保存。与上轮修的编剧/分镜**完全同型**，只是这个文件没碰。修：改 `performOrThrow` + `StoryStage` try/catch。**反查全部保存点后断言收紧为**「没有任何 `save-*` 标签走 `perform`」——`select-intro`/`confirm-assets` 保留吞异常是**正确**的（失败只不推进游标，无草稿残留）。
- **E【中·隔离失败仍静默清空】** `rename(...).catch(() => undefined)` 失败后照样 `emptyStore()` → 坏文件没被隔离、下次保存照样覆盖。上一轮「坏数据留证」**在失败路径不成立**。修：隔离失败退化为 `copyFile`，连复制也失败则**拒绝启动**。**现在真的可测**：把隔离目标预先占成**非空目录**，`rename`/`copyFile` 会可移植地双双失败。
- **F【低·F13 分类碰撞】** 声明顺序即优先级会错归。**证据分层**：服务端 `llmTextStages` 对 `category` 做枚举精确校验、要求 `assetKey` 前缀等于 category → **只在手改/历史产物上可能发生，不是活缺陷**。规则收敛为「精确 → **最早出现** → 最长词 → 声明顺序 → `assetKey` 前缀 → 原值」。诚实说明：审查举的「场景中的生物」本身是歧义句，我按**规范输出格式**（`<规范名>：<子方向>`）取「早者优先」，而非词尾中心语优先。

**方法学升级（本轮最有价值的东西）**：写了 `output/positive-controls.mjs`——**逐条重新注入缺陷、要求对应 smoke 确实变红、然后按内存副本逐字节还原**（不用 `git restore`，以免毁掉未提交的修复）。新增 9 条断言 **9/9 被捕获**；第一轮就有 **1 条 MISSED**，正是上面那条 `includes` 空转。**「命中率 0 漏」是必须实测出来的，不是设计出来的。**

**我这轮自己犯的两类操作失误（记账）**：
1. **同一文件并行编辑被覆盖**：对 `AssetPlanStage.tsx` 并行发两个 `Edit`，改写 `categoryKey` 函数体的那次被覆盖丢失，只剩调用点改了。**幸而探针立刻显示了错误分组**（旧逻辑仍生效）才没把「改了」当「修好了」。→ **同一文件不要并行编辑**；改完必须用**行为证据**确认，别相信编辑回执。
2. **探针误改真实会话**：`probe-boundary-escape.mjs` 首版用 `page.click("button.vbs-primary")`，在「面板未出现」的失败路径上点到了**步骤自身的**「选择此方案」，把 `ses_73d99e7d` 的课程导入确认了、游标推到 `STORY_SCRIPT`。已用 `restart-from COURSE_INTRO_SELECTION` 还原，并把点击改为 `getByRole("button", { name: "返回任务列表" })` **按可访问名精确绑定**。→ 破坏性探针在**失败路径上也必须只读**。

**真实浏览器验证**：`output/probe-boundary-escape.mjs`（按当前游标阶段临时打断映射 → 加载页面 → 断言面板/头部/按钮 → **点击并断言跳转 `/gallery`** → `finally` 还原 + sha 校验）。结果：面板 1、可见头部 1、`back="任务列表"`、`primary="返回任务列表"`、**0 pageerror**、点击后到 `/gallery`。

## 第七轮：前端体检与优化（a2a5b2f 之后）

- **Fix 1 轮询条件请求**：`/api/state` 全量 1.3 MB、每 5s 轮询 ≈ 899 MB/小时；服务端有 ETag 但浏览器轮询不发 If-None-Match。`api.ts` 加 `stateConditional()`（模块级 last ETag），只改 5s 轮询，其余 ~20 处一次性读写不动 → 5 次 200+4×304(0B)，轮询流量归零。
- **Fix 2 HTTP 压缩**：新增 `src/server/responseCompression.ts`（node:zlib 窄中间件，br 优先，<1KB 跳过、流式直通、媒体白名单、失败降级），挂到 `index.ts` 的 cors 之后。正确性校验 `output/verify-compression.mjs` **40/40**（解压逐字节一致/MP4 透传/Range 206/304 共存/br;q=0 协商）。首屏 2379 KB → **480 KB（4.96×）**。
- **Fix 3 a11y**：`.session-delete` 两处加 `aria-label`（App.tsx）。
- **不改的三项**：单包 267 KB 是 `inline-entry-assets.mjs` 刻意内联（无文档说明理由，只报告）；`rawPrompt===prompt` 重复 gzip 零成本；进度条裁切是画布溢出假阳性。
- **方法论坑**：① Node `fetch` 自动解压，测压缩响应必须用 `node:http` 原始字节手动解压——第一版校验脚本因此误报「解压失败」；② runtime 探针「无 alt 图片 11」口径偏保守，真实清单只有装饰性 `alt="" aria-hidden`。
- verify:offline 全绿。报告：`output/frontend-health-2026-09-16.md`。

### 第六轮收口（同日）

- **回归网**：`npm run verify:offline` **RC=0**（1m56s，日志 `output/verify4.log`；含新增的
  `concurrent store save` / `asset-confirmation-gate` 两条，末尾 `ALL UNIT CHECKS OK`）。
- **提交**：`a2a5b2f`（分支 `feature/videosbatch-audio-readiness-gate`，15 files，+569/-178）。
  上一轮 `4d0379b` = 1.4.10；本轮 spec 升 **1.4.11**（§7.13 闸门对齐 + 新 §7.14 客户端失败
  可见性 / §7.15 存储不可读时隔离与拒绝启动）。ADR-0001 §16 记录本轮与两处操作失误。
  `output/positive-controls.mjs`、`output/probe-boundary-escape.mjs` 属 gitignore，不入库。
- **技能沉淀**：`videosbatch-stage-development` SKILL.md 新增
  **坑 2d**（闸门 ready 判定必须校验阶段自身 `status`，谓词与 UI 双读 status 时不能只看产物）、
  **坑 5**（错误边界不能用 SSR 验证——`renderToStaticMarkup` 遇子组件抛错直接 throw，
  不走 `getDerivedStateFromError`；须真实浏览器 + 按 accessible name 定位，或单元级
  实例化边界；禁用宽泛 `includes`）、**坑 6**（阳性对照纪律：重注入→必红→按内存副本还原，
  禁用 `git restore`）；顺手修掉重复的「坑 2b」编号（→ 2c）与「三大高频坑」标题。
- **环境恢复**：dev server 重启成功（`tsx src/server/index.ts`，5173，`/api/state` HTTP 200 in 34ms）；
  `runtime.videosBatch = {executorMode:fake, mediaMode:fake, ttsProvider:fake}` 符合预期；
  探针会话 `ses_73d99e7d` 已确认干净恢复：`currentStage=COURSE_INTRO_SELECTION`、
  `introLocked=false`、`selectedIntroId=undefined`。

## 第八轮：前端设计审查（只审不改）

- 方法：真实截图（gallery / stage02 / stage09）+ token 全量实测（`output/design-review-capture.mjs`，
  截图在 `output/design-review/`）。
- **总评**：纸面+黄铜身份成立、强调色纪律好（黄铜仅 ~4%）；幽灵数字 + 9 步进度轨是签名元素。
- **P1**：①三套主题 token 并存（`vbs-v2` 纸面 / `studio-*` 暗黑 / `focus-*` 暗黑 teal）+ Inter 与
  system-ui 两套 sans——下一轮改色前先盘点各作用域消费者；②空状态浪费（gallery 空态无 CTA、
  stage09 大空卡）；③标题自我重复（gallery 一屏 3 次「Gallery 创作广场」、stage09 eyebrow==H2）。
- **P2**：演示 banner 与进度轨形成双琥珀带；Session/session/会话 词汇混用；stage09「进入制作画布」孤儿按钮。
- **P3 不动**：system-ui 字体栈（单文件内联构建的体积预算约束）；stage02 单列卡片是阅读决策。
- 报告：`output/frontend-design-review-2026-09-16.md`。**均未动手，等大哥点单。**

## 第九轮：设计审查落地（「处理吧」）

- **Gallery**：hero 去掉与 topbar 重复的 H2（单 H1）；空态补 CTA「去画布生成第一个成片」（`galleryEmptyCta`）；`.gallery-hero h2` 三处死规则清除。
- **StagePage**：kicker 与标题同文时自动去重（仅 final 命中；SSR 断言 intro kicker 仍在、final 消失）。
- **FinalVideoStage**：`vbs-final-actions` 移入交付卡片内（原先孤儿按钮浮在卡与 footer 之间；`margin-top:18px` → 0，由 grid gap 接管）。
- **ModeBanner**：移到进度轨下方（消除双琥珀带），样式降噪为白底细线行，`#e3d4ae`/`#6a6047` 收进 token。
- **文案**：i18n 中文侧 Session→会话 统一 17 处（批量脚本带断言计数 `output/fix-i18n-copy.mjs`；`.seereel-session 包` 等格式名保留）。
- **spec 1.4.12**：新增 §7.16 主题作用域清单（三套 token + 两套字体族的消费者、覆盖层纪律、删作用域前必须盘点）。
- 验证：tsc 干净；4 条相关 smoke 过；`verify:offline` RC=0；真实浏览器复拍断言（gallery 单标题+CTA、final kicker 无/banner 在轨下白底/动作入卡）+ 截图 `output/design-review/after-*.png`。


## 第八轮：信息密度减法（大哥指令「减少一些信息密度」）

定位热点靠复拍截图 + 逐处读码（不凭印象）。四处同义/冗余全部收敛：

- **ModeBanner 两行→单行带**：说明句缩为「示例内容，可放心操作。」；CSS body column→row+baseline，
  分隔符「·」由 span::before 提供（文案不掺标点），45px 高。
- **最终成片页 4 处同义反复→1 处**：banner（全局保留）/卡片正文「这里是示例内容，不会生成真实视频文件」→
  「全流程已走完，当前为演示内容。」/占位「示例成片，暂无视频文件」→「示例成片」/
  small「尚未生成」→ 仅在有真实 progress/url 时渲染（fake 下不渲染）。
- **Gallery 空态文案**：删掉指向不存在按钮的「点击发布到 Gallery」，改为准确路径描述（中英双语）。
- **明确不动**：进度轨（序号+名称编码真实顺序，是签名元素）、WorkflowFooter（本就是替代旧重复工具条的产物）、
  LessonStage 自建 header（要放 Tabs.List，结构与其他阶段一致）、SeeReel 顶栏 legacy 区（历史 P3）。

- 验证：tsc 干净；ui-foundation/guided-v2/content-ux/client-routes 四 smoke 过；
  真实浏览器断言 7/7（banner 同行+45px、占位无 small、正文单句、空态文案）+ 截图 density-*.png；
  verify:offline RC=0。
- 坑：density-after 探针首跑 2 条 FAIL 是 **/canvas 不带 id 回退到最后一个 session**，该会话不在 final 步——
  断言脚本必须显式导航到目标会话（/canvas/<id>），不能依赖回退。

## 第九轮：布局横向化（大哥指令「不要垂直布局，高度被拉低」）

实测（1366×768）：页面总高 1222px、成片占位框一个 579px、内容 306px 才开始——这就是「高度被拉低」。
两处横向化：

- **成片卡改双列 grid**（copy+actions 左列 / 播放器右列，grid-template-areas）；播放器/占位加
  max-height:440px。1366 宽下：占位 579→387，卡片 775→437，页高 1222→839（≈一屏）。
- **ModeBanner 从独立横带改为进度轨同一行的 chip**（aside，icon+单行省略文字，说明句进 title；
  弹性上限 flex:0 1 320px）。省一条 45px 横带，且不与轨争宽度——轨 9 站名全亮。
  ≤960px chip 落到轨下成细线行（先踩过一次坑：规则最初落进 640 块，820px 不生效，
  已挪到 960 块并实测 820/620 两档）。

**坑（CSS 级联）**：v2 区有一条把 banner 与 progress-wrap 一起按 gutter 加左右内边距的旧整幅带规则，
chip 继承后 320px 被 286px 内边距吃光、strong 宽 0 文字消失——「元素进了新布局容器后必须重查所有旧
作用域规则」。计算样式探针（getBoundingClientRect + computedStyle）比肉眼截图快得多。
已把 banner 从该选择器组摘出。

验证：tsc 干净；四 smoke 过；浏览器实测 1366/820/620 三档；verify:offline RC=0（verify-layout.log）。

## R12 垃圾信息减法（1024185）

用户「减少一些没有用的垃圾信息和展示信息」。基于真实会话数据（ses_9c499a32 全管线）盘点，
删 9 类低价值展示：分镜「已同步制作画布」×12 同义 pill、摘要「00–10s · 10s」时长双写、
提示词页 asset_ 内部 id chips、执行页单数字「计划片长」卡 + 与头部 N/M 重复的进度卡、
教案只读「项目/标题+字数」头行、资产页 P001-A001/CHARACTER- 内部编号、课程导入「组内推荐」
badge（理由文本保留）、故事「故事类型」元数据、剧本 duration/10 拍脑袋「预计主分镜」。
CSS 死规则同轮清掉（progress-card/recommend/native-pill/lesson-header，两套样式表都清）。
sessionTitle 沿 LessonStage→StageWorkspace→Studio 链拆除（Studio 级保留）。

坑（再次踩实）：同文件多个 Edit 并发会互相覆盖且回执仍是成功——guidedStudioV2.css 三连发
只活了一个；**同文件编辑必须逐条顺序发**。smoke-videosbatch-content-ux 不在 verify:offline
链里（历史遗留，本轮只改了它的 ExecutionStage 调用）。

验证：tsc --noUnusedLocals 0；浏览器探针 19/19（output/verify-junk-removal.mjs，成片页高度
仍 839px）；content-ux/product-ui-foundation/copyable-prompt-ui PASS；verify:offline rc=0；
净变化 +6/−89。dev server 已恢复（5173，fake 三件套）。

## R13 呼吸感与质感（09976c7）

用户「优化布局，增加呼吸感和高级质感」。全程在 token/既定规则内放宽，不新增规则：
- 空间：workspace 上 36/下 76（紧凑层 28）、站牌 20/32、页脚 18/10+margin 34、
  卡片 padding 16→20~24、卡片间 gap 12→16、按钮 38px 高。
- 质感：radius token 卡 14→16 / 控件 9→10（tokens.css 单一来源）；
  成片卡独享 --vbs-v2-shadow-raised——深度只给交付物，其余保持「纸面平铺」设计语言。
- 关键发现：紧凑间距真身在 guidedStudioV2Focus.css 的 @media(max-height:950px) 块里，
  768 高必命中；改间距要同时看 guidedStudioV2.css 基础层 + Focus 层两个文件。

实测 1366×768：成片页 839→879（<900 预算）；820/620 无横向溢出、单列折叠正常；
verify:offline rc=0。dev server 已恢复。

## R14 流式间距 + 渐进式披露（4df8a58）

用户「合理的空间布局，间距适应好。做响应式设计。渐进式披露」。两件事：

**流式间距**：tokens.css 新增 5 个 clamp token（pad-card/pad-hero/gap-card 按宽度，
pad-work-top/bottom 按高度），gutter 底部流式化 max(clamp(18,2.4vw,28), measure 派生)。
删掉了所有被 token 取代的断点固定值（v2 三处 workspace padding、960 块多处 18px、
640/960 final-delivery padding、Focus 层 32/64 + 短屏 padding-top）。实测 gutter
420→143→50→24.6→19.7→18、卡片 padding 28→24.2→21→18 全程连续无跳变。
要点：**工作区上下留白用 vh 驱动**（保护的是高度），卡片用 vw 驱动。

**渐进式披露**：
- 新组件 `components/Clamp.tsx`：max-height 收敛（不是 line-clamp——line-clamp 对块级
  子元素会把每个子块当一行计数，多段落场景必坏）+ 渐隐 mask + 溢出才渲染开关。
  用法 `<Clamp lines={4}><p>…</p></Clamp>`，children 原样透传所以旧选择器全兼容。
- 铺点：课程导入卡正文 4 行、资产计划卡描述 3 行（短文本自动无开关）。
- 剧本页改成与分镜一致的手风琴（Radix Accordion，复用 vbs-storyboard-* 通用类）：
  默认展开第 1 场、标题行=编号徽章+场景名+知识重点摘要、编辑态 value 全开、
  收起集合记住用户选择。3883→1985px。
- 教训：**line-clamp 收敛不了「本来就只超一点」的内容**（剧本单场景 ~250px vs 8 行限制
  250px，只省 7px）——结构重复的审阅文档该用手风琴，单字段长文才用 Clamp。

验证：8 档位（1920~375）无横向溢出；tsc --noUnusedLocals 0；三条 smoke PASS；
verify:offline rc=0；+199/−80。dev server 已恢复（5173，fake 三件套）。

## R15 全量扫荡（8c9f4f9）

用户「你并没有用尽你的全力」。补上 R14 没铺完的：

**渐进披露补全**：故事只读全文 Clamp 14 行（888px 摊开→收敛）；资产计划卡的
出处/连贯性/提示词三块参考信息合并为一个默认收起的 details（vbs-plan-reference），
卡片默认只剩 名称+3 行描述；导入卡删 min-height 260；分镜标题行 74→62px。

**响应式断点修正（本轮最大收益）**：960/899 过早降单列导致平板爆高——820px 下
生成页 7242、资产图 4034。改为 899~641 保持 2 列、640 以下才单列：
820px 实测 生成 7242→2747、资产图 4034→2522、导入 4113→3615、计划 3053→2474。
display 字号 clamp(22px,1.95vw,27px)。

**工具**：8 档位 × 9 步矩阵探针 output/matrix-r15.mjs（页高+横向溢出一次出全量数据）。
验证：3 smoke PASS + verify:offline rc=0（注意：Bash shim 坏时 npm 会劫持 wsl.exe 被沙箱拦，
verify 链改 PowerShell 跑通）。dev server 恢复 5173。

## R16 深度精修（246c582）

用户「继续优化，优化深度不够」。这轮不砍高度，做质感层审计 + 精修，全部探针驱动
（output/audit-depth-r16.mjs：字号/字重/字体族/transition 分布 + WCAG 对比度扫描 + 行宽）：

- **对比度**：探针扫出 6 项不达标（最差 1.81——头部「·」分隔点、序号 9px 2.7、
  mode-switch 4.21、导语 4.1）。根修 = `--vbs-v2-text-soft` #6c7075→#62666a（要按**最暗底
  canvas #eae9e3** 算 AA，不是白底）。清后仅剩站牌 40px 水印（aria-hidden+user-select:none
  装饰豁免，保留 faint）。
- **假字重**：650 在 system-ui（非可变字体）上被浏览器静默取整，10 处 → 600。
- **字号 token 化**：v2 内 14 处 11/12px 硬编码 → size-label/micro；9px 三处灭掉（序号升
  micro）；新增 size-headline clamp(19,1.65vw,22) 收编成片卡标题/教案面板 h1、size-brand
  19px。探针的 transition "0.12s/0.09s" 元组是 UA 内置假信号，勿追。
- **中文行宽**：教案 pre 实测 63 字/行（超 35–45 舒适区）→ max-width 44em + 居中；
  故事 longform 同步。CJK 行宽用 em 算（1 字=1em），别用 ch。
- 教训：对比度审计要**枚举失败元素**（含父类名/文案），只报数量没法修。

验证：对比度探针 6→1（唯一剩装饰）、矩阵 8 档位无回退、tsc 0、3 smoke PASS、
verify:offline rc=0（21 smokes）。dev server 恢复 5173。

## R17 系统性审查（报告交付，未改代码）

用户要全面系统审查 + 可执行方案。产出 `output/frontend-systematic-review-2026-09-16.md`。
核心实测结论：运行时全绿（空闲 0 longtask/0 变异、轮询 10s 仅 2 次 304、canvas 240 节点
51MB）；**最大债 = vite `inlineDynamicImports:true` + cssCodeSplit:false 单文件构建废掉全部
code-split**（875KB 单 chunk gzip 267KB 全内联进 1MB index.html，VideosBatchStudio 静态导入，
FlowView 的 lazy 被吞）；Inspector.tsx 4225 行/93 hooks；stages any×42；三主题并存量化
（v2 内仍 50hex+19rgba，styles.css 5033 行）。探针脚本：system-review-runtime-r17.mjs、
poll-identify-r17.mjs、audit-depth-r16.mjs、matrix-r15.mjs。
关键坑：首次 runtime 探针 87 reqs/8s 是跨导航把 vite 模块加载算进去的假信号，单路由实测
只有 2 次/10s——**轮询审计必须单路由隔离测**。路线图：P1①code-split（路线 A 弃单文件/
路线 B 双入口）、②Inspector 拆分、③artifact 类型、④主题三合一；P2 skip-link/aria、
i18n 二选一、媒体懒加载+键盘走查（上线前）。

## R18 审查问题深度优化（P1-① + P2 无障碍落地）

- **P1-① code-split 落地**：vite.config 删 `inlineDynamicImports`（cssCodeSplit 也一并删了，
  单 CSS 内联形态）+ `rollupOptions:{}` 空壳减掉；App.tsx 的 VideosBatchStudio 改 lazy+Suspense
  （与 FlowView 同构）。`inline-entry-assets.mjs 无需改`——只内联入口 chunk/CSS，动态分包留在
  dist/client/assets 运行时按需加载。实测：首屏 JS gzip 267.3→128.5KB（−52%），总传输
  ≈296→155KB（−48%）；Studio 29.2KB / FlowView 104.9KB 按需。
- **smoke 断言过时教训**：`product-ui-foundation` 用源码字符串钉**静态导入**，lazy 后如实变红
  ——已改为钉 lazy 接线正则（防回退到静态导入让分包静默失效），并走完注入→变红→sha256
  逐字节还原（别用 git restore）纪律。
- **P2 无障碍**：main 顶部 skip-link（i18n 双语 `skipToContent`，默认 translateY(-200%)
  藏出屏）+ workspace 加 `id=workspace-main tabIndex=-1`（抑制 programmatic focus outline）+
  隐藏 file input 补 `aria-hidden tabIndex={-1}`；FlowView 两个 `display:none` input 天然脱离
  a11y 树，**不动（防过度修复）**。
- **验证链**：tsc rc=0、build 4 chunks、verify:offline rc=0（日志核对 55 passed/0 失败标记，
  防 && 链早断假绿）。
- 深度方案文档 `output/frontend-review-depth-solutions-2026-09-16.md`：P1-① 已落地含证据；
  P1-②③④ 与 P2-i18n/懒加载/键盘走查为根因级执行方案待大哥拍板（顺序建议 ③→②→④）。
- **PowerShell 工具输出吞没**：本会话 PowerShell 的 stdout 全部被吞（连 node console.log 都
  看不见）， workaround 是所有探针统一 `fs.writeFileSync` 落盘到 output/ 再 Read。

## R18 收尾：前端经验沉淀为两个用户级 skill

大哥拍板拆两个：`~/.workbuddy/skills/frontend-design-standards`（设计要求：减法优先、clamp
流式间距、渐进披露、排版硬指标、skip link 形态、8 档响应式矩阵）+
`~/.workbuddy/skills/frontend-review-discipline`（审查与验证纪律：三源审查法、断言注入→变红
→sha256 还原、smoke 必须在链内、inlineDynamicImports 静默吞 lazy、轮询单路由隔离、巨型组件
先 hooks 后组件、证据落盘）。均为项目无关写法，VideosBatch 案例只作佐证。
环境坑（PowerShell 吞 stdout 等）留在 skill 第 5 节 + 用户级 MEMORY，未单独成 skill。

**修正（同晚）**：大哥指出已有全局 skill `hb-design`（操作台口径：信息取舍/文案/按钮可用性/
判据门禁）。对照后收口重叠：frontend-design-standards 删掉「减法优于补丁」节（hb-design §一
更深），改为分工边界指过去；frontend-review-discipline §1 加互引（hb-design §六管上屏类判据，
本 skill 管审查方法/构建/环境）；hb-design 的分工表补两行指向新 skill。**教训：建新 skill 前
先扫 `~/.workbuddy/skills/`（含 junction 的 agents 侧）现有技能，按分工表归位而不是新开。**
